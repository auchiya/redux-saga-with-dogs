# redux-saga-with-dogs

### created using vitejs: <https://vitejs.dev/>

# running the project

``` powershell
git clone https://github.com/auchiya/redux-saga-with-dogs
cd redux-saga-with-dogs
npm install
npm run dev
```

check the output and find the localhost url (/localhost:{PORT})